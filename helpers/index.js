const { signToken, sendEmail, sendForGotEmail, sendResetEmail } = require('./helpers');
const helpers = {
    signToken,
    sendEmail,
    sendForGotEmail,
    sendResetEmail
}
module.exports = {
    helpers
}
